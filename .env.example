# Copy this file to .env and edit values as needed.

# Database connection (used by the PHP backend)
DB_HOST=mysql
DB_PORT=3306
DB_NAME=app
DB_USER=app
DB_PASS=app

# Example: override to connect from host machine
# DB_HOST=127.0.0.1
# DB_PORT=3306